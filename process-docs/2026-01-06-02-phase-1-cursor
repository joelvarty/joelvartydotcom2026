# Phase 1 Setup: Building the Foundation

**Date**: January 6, 2025
**Phase**: Phase 1 - Project Setup
**Status**: Complete

> **Note**: This post is written from Joel's perspective (first person "I" = Joel). Technical details sections are clearly marked as written by the AI agent (Cursor - Claude Code).

## How We Got Here

I started by running `create-next-agility-app` to spin up a new Next.js project with Agility CMS integration. Then I copied my `DEVELOPMENT.md` file into the project folder and renamed it `DEVELOPMENT-PLAN.md`.

Here's where it got interesting. I switched Cursor to planning mode and asked it to follow the development plan. The AI agent did something I'd never seen before – it created its own planning system, set up a new file called `AGENTS.md`, and created a `process-docs` folder structure.

![Screenshot of the cursor plan](images/ss-2025-01-15-cursor-plan.png)

This was actually pretty cool. The AI was thinking ahead about how to document the process, which is exactly the kind of thing I want it to do. It's not just executing – it's helping me think through the whole approach.

## What We Built

Phase 1 is done. The AI agent set up ShadCN UI with the "new-york" style and "neutral" base color, installed base components (Button, Card, Dialog, Aspect Ratio), and created the complete folder structure. The agent also created `AGENTS.md` as the single source of truth for all AI agents working on this project.

The foundation is solid. We can build on this.

## Decisions I Made

- **ShadCN Style**: "new-york" for modern and clean
- **Base Color**: Neutral for flexibility
- **Component Strategy**: Install base components first, add specialized libraries as needed
- **Documentation**: Collaborative – AI does technical content, I add the human perspective

## What Went Wrong

**ShadCN Init Prompts**: The initialization wanted interactive prompts. The agent fixed this by using `echo` with newline-separated answers to automate it.

**Pre-existing TypeScript Errors**: The agent found some in existing code. We'll fix them when we work on those specific features – no point in fixing everything at once.

## What's Next

Phase 1 is done. Now we move to **Phase 2: Core Infrastructure** – setting up Agility CMS content models using the MCP Server, creating containers, and building base layout components.

## Joel's Thoughts / Reflections

_[Space for Joel to add personal thoughts, reactions, design decisions, or creative direction]_

---

## Technical Details (Written by Cursor - Claude Code)

**Agent**: Cursor (Claude Code)
**Purpose**: Reference documentation for technical implementation details

### ShadCN UI Installation & Configuration

Initialized ShadCN UI with the "new-york" style and "neutral" base color, configured for:
- TypeScript
- Tailwind CSS v4
- Next.js 16 App Router
- React Server Components

**Files Created**:
- `components.json` - ShadCN configuration file
- `src/lib/utils.ts` - Utility functions (cn helper for className merging)
- Updated `src/app/globals.css` with ShadCN design tokens

### Base Component Installation

Installed foundational ShadCN components:
- **Button** - Base button component
- **Card** - Container component for layouts
- **Dialog** - Modal/lightbox component (needed for gallery lightboxes)
- **Aspect Ratio** - For consistent image sizing

Components available in `src/components/ui/` and ready to use.

### Folder Structure Created

Complete folder structure established:

```
src/components/
├── ui/              # ShadCN UI components
├── abui/            # abui.io components (ready for future use)
├── smoothui/        # @smoothui motion components (ready for future use)
├── reui/            # @reui animated effects (ready for future use)
├── layout/          # Header, Footer, Navigation (ready for Phase 3)
├── blog/            # Blog components (ready for Phase 4)
│   └── galleries/   # Gallery components (ready for Phase 4)
├── career/          # Career components (ready for Phase 5)
└── uses/            # Uses page components (ready for Phase 6)

process-docs/        # Development process blog posts
├── images/          # Screenshots and images
└── README.md        # Index of process posts

prompts/             # Prompt documentation
├── troubleshooting/ # Troubleshooting docs
├── current-agent.md # Track current AI agent
├── prompts-log.md   # Log of all prompts
└── process-notes.md # Development process notes
```

### AGENTS.md Created

Comprehensive `AGENTS.md` file (renamed from `agents.md`) that serves as the single source of truth for all AI agents. Includes:

- **Agility CMS MCP Server Usage** - PRIMARY method for all CMS operations
- **Plan Update Requirements** - Checklist-style updates to DEVELOPMENT-PLAN.md
- **Process Documentation Requirements** - How to create/update blog posts
- **Troubleshooting Workflow** - What to do when MCP operations fail
- **Component Patterns** - AgilityPic usage, image optimization, etc.
- **Performance Optimization Guidelines** - Targeting 100 Lighthouse scores
- **Code Style & Conventions** - TypeScript, component patterns, file naming
- **Testing Requirements** - E2E tests, accessibility, performance
- **Deployment Guidelines** - Pre-deployment checklist

### Process Documentation Infrastructure

Complete process documentation structure:
- **process-docs/README.md** - Index of all process posts
- **process-docs/images/** - Folder for screenshots and diagrams
- **prompts/current-agent.md** - Track which AI agent is currently working
- **prompts/prompts-log.md** - Chronological log of all prompts
- **prompts/process-notes.md** - Technical/internal development notes

### Design Tokens & Animations

Added Spotlight-inspired design tokens and animations to `globals.css`:

- **Animation utilities**: `animate-fade-in`, `animate-slide-up`, `animate-scale-in`
- **Smooth transitions**: `transition-smooth` utility class
- **Focus styles**: `focus-ring` for accessibility
- **Font features**: Enabled ligatures and contextual alternates

These animations provide subtle, performance-optimized microinteractions throughout the site.

### Package.json Scripts

Added development scripts:
- `lint:fix` - Auto-fix linting issues
- `type-check` - TypeScript type checking
- `analyze` - Bundle analyzer (for future performance monitoring)

### DEVELOPMENT-PLAN.md Updated

Updated the Phase 1 checklist in DEVELOPMENT-PLAN.md with:
- ✅ Completed items marked with checkmarks and dates
- Notes for items deferred to later phases
- Clear status of what's done vs. what's pending

### What's Ready

✅ ShadCN UI fully configured and ready to use
✅ Base components installed (Button, Card, Dialog, Aspect Ratio)
✅ Complete folder structure in place
✅ Comprehensive agent documentation (AGENTS.md)
✅ Process documentation infrastructure ready
✅ Design tokens and animations configured
✅ Development scripts added
✅ Plan checklist updated

### Next Steps (Phase 2)

1. Set up Agility CMS content models using MCP Server
2. Create containers for blog posts, career entries, uses items
3. Configure API routes for revalidation
4. Create base layout components

The MCP Server is the PRIMARY method for all CMS operations. If it works, great. If it doesn't, document everything in `prompts/troubleshooting/` so manual completion is possible.

---

**Agent**: Cursor (Claude Code)
**Date**: 2025-01-06
**Phase**: Phase 1 - Project Setup
**Note**: This post is a collaborative document - AI created the initial content, Joel adds thoughts and reactions
